<%= form_with(model: organism, local: true) do |form| %>
  <% if organism.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(organism.errors.count, "error") %> prohibited this organism from being saved:</h2>

      <ul>
      <% organism.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :species %>
    <%= form.text_field :species, id: :organism_species %>
  </div>

  <div class="field">
    <%= form.label :tax_id %>
    <%= form.number_field :tax_id, id: :organism_tax_id %>
  </div>

  <div class="field">
    <%= form.label :type_of_web %>
    <%= form.text_field :type_of_web, id: :organism_type_of_web %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
